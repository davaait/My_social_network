let initialState = {
    friends: [
        {id: 1, name: 'Ksusha'},
        {id: 2, name: 'Dmitry'},
        {id: 3, name: 'Olga'}
    ]
}

const sidebarReducer = (state = initialState, action) => {

    return state;
}

export default sidebarReducer;